"""Training: making the simulator differentiable (surrogate gradients),
and both training paradigms — surrogate-gradient BPTT and local learning
rules (STDP, SuperSpike; e-prop only if reached, per the roadmap's
de-risking of Week 11)."""
