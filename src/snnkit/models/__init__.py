"""Neuron model library: equation definitions + default parameters for
specific, named neuron models (LIF, Izhikevich, ...), for use with the
`snnkit.api.NeuronGroup` object API and `snnkit.core.parser`.

`adex.py` is intentionally absent — AdEx was deferred out of v2 scope per
the roadmap critique (LIF + Izhikevich judged sufficient for Phase 2).
"""
