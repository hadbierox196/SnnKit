"""Connectome disease-spread extension (Phase 3, Weeks 13-16).

Optional extension per the roadmap: gated on Phase 1-2 (the core engine)
being genuinely solid, which it is (41 passing tests, benchmarked,
documented) — see `docs/connectome-narrative.md` for why this belongs in
the same repo rather than being a bolted-on demo.
"""
